# Keep local mode as the safe default. V0.4 data continues to work without a backend.
VITE_BACKEND_PROVIDER=local

# Supabase is the primary V0.5 backend for the Vercel deployment.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# CloudBase PG is deferred until the mainland-China phase.
VITE_CLOUDBASE_ENV_ID=
VITE_CLOUDBASE_PUBLISHABLE_KEY=
VITE_CLOUDBASE_REGION=ap-shanghai
