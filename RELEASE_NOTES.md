# Future With You V0.4.0 Release Notes

发布日期候选：2026-08-19
发布方式：`dev → main` Pull Request，经人工 Code Review 后手动合并与上线

## 这一版的主题

V0.4.0 将“做过的愿望”升级成能够持续生长的共同故事。每段回忆拥有一个统一数据源，同时出现在爱情时间轴、我们的宇宙和恋爱博物馆中。

## 新增功能

### 爱情时间轴

- 以情侣纪念日作为故事起点。
- 按实际日期排列手动回忆与已经实现的愿望。
- 在过去与未来之间加入“此刻的我们”节点。
- 将时间胶囊接到时间轴未来方向，显示距离开启的天数。

### 我们的宇宙

- 每段回忆自动成为一颗位置稳定的星。
- 星星颜色由故事类型决定；珍藏展品会成为更明亮的大星。
- 星间带有星座连线、星云、呼吸与微光动画。
- 点击星星可打开完整故事并继续编辑。

### 恋爱博物馆

- 用私人展厅形式展示照片、聊天截图、礼物和文字故事。
- 支持将任意手动回忆设为“珍藏展品”。
- 未指定展品时自动陈列最近故事，不会出现空白展厅。
- 展品使用画框、铭牌、编号和走廊光影完成沉浸式呈现。

### 回忆收藏器

- 可录入标题、日期、地点、故事类型、记录者、正文、标签和一张封面。
- 图片在浏览器中压缩后保存。
- 支持新增、查看、编辑和删除手动回忆。
- 首页加入“那年今日”，在相同月日唤起往年故事。

## 愿望与回忆联动

- 完成愿望时自动创建一段关联回忆。
- 再次编辑完成信息会更新原回忆，不会生成重复星星。
- 在故事编辑器中修改关联回忆，收藏页的日期、文字与照片会同步更新。
- 关联回忆不能被单独误删；在收藏页恢复愿望为待做后，关联故事才会一起移除。
- 手动回忆与愿望完成状态保持独立。

## 数据与兼容性

- 应用状态升级为版本 3，存储键为 `future-with-you.app-state.v3`。
- 自动读取并迁移 V0.1、V0.2 与 V0.3 本地状态。
- 旧版已经完成的愿望会在首次打开 V0.4 时生成关联回忆。
- 迁移后，关联照片和文字只保存在统一回忆实体中，避免 localStorage 重复占用。
- 完整 JSON 备份继续兼容旧状态导入；新备份会标记状态版本 3。
- 备份仍带 SHA-256 完整性校验。

## 体验与质量改进

- 底部导航新增“故事”一级入口；收藏与回忆清单仍可从愿望页进入。
- 首页快捷入口改为“故事宇宙”。
- 新增浪漫星空、星云、展厅、画框、时间线、珍藏星与那年今日动效。
- 支持深色模式、窄屏布局、触控热区和 `prefers-reduced-motion`。
- 修复弹窗标题的 `aria-labelledby` 关联，改善读屏体验。
- PWA 离线缓存名升级到 `future-with-you-v0.4.0`，确保发布后刷新旧缓存。
- 项目、lockfile 与运行脚本版本统一为 `0.4.0`。

## 验证结果

- `npm run build`：PASS
- `node --check scripts/mobile-modal-regression.mjs`：PASS
- `npm run test:mobile -- http://127.0.0.1:4173`：PASS

移动端真实 Chromium 回归确认：

- V0.3 状态成功迁移为版本 3。
- 已完成愿望生成 1 段关联回忆。
- 时间轴、星空和博物馆均展示该回忆。
- 真实移动端表单成功创建珍藏回忆，并在博物馆首位展示。
- 迁移后旧照片与文字字段已压缩为单一数据源。
- 完整备份状态版本为 3，SHA-256 校验通过。
- 390 × 430 低高度视口下弹窗无裁切，内容可滚动，提交按钮可见。

## Code Review 建议路径

1. 从 V0.3 的浏览器状态打开 V0.4，确认收藏、完成、照片和时间胶囊仍在。
2. 完成一个愿望，检查收藏页和故事页是否各出现一次。
3. 在故事页编辑该回忆，确认三个视图与收藏页同步。
4. 手动新增一段珍藏回忆，检查大星星与博物馆优先展示。
5. 下载完整备份，检查摘要中包含故事回忆数量。
6. 在手机上切换浅色/深色与“减少动态效果”后检查可读性。
7. 合并前确认 PR 目标为 `main`，来源为 `dev`。

## 已知边界

- V0.4.0 仍使用当前设备的 localStorage，不提供双人实时同步。
- 账号、云数据库、共同相册、推送通知和跨设备冲突合并需要真实后端项目及部署密钥，本次没有伪装实现。
- 每段回忆当前保存一张封面；多照片相册适合与后续对象存储一起实现。
- 清理浏览器数据、卸载 PWA、换域名或换设备前仍需先下载备份。

## 上线控制

本次只推送 `dev` 并创建 Pull Request，不自动合并 `main`，也不自动触发生产发布。最终上线由代码审查者手动决定。
